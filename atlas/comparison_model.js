(function(root,f){if(typeof module==='object'&&module.exports)module.exports=f(require('./capacity_model.js'));else root.WaterComparison=f(root.HydrogenCapacity);})(typeof globalThis!=='undefined'?globalThis:this,function(H){
function balance(input){const result=H.assess(input);if(result.status==='missing')return {status:'missing',demand:result.intakeM3Day,waterBudget:null,margin:null,result};const periods=result.periods.map(p=>({...p,waterBudget:p.supply-p.existing-p.reserve,margin:p.supply-p.existing-p.reserve-p.intake}));const worst=periods.reduce((a,b)=>a.margin<=b.margin?a:b);return {status:'scenario',demand:result.intakeM3Day,waterBudget:worst.waterBudget,margin:worst.margin,preexistingDeficit:worst.waterBudget<0,limitingPeriod:worst.period,verdict:Math.abs(worst.margin)<1e-9?'At budget':worst.margin<0?'Shortfall':'Margin',periods,result};}
function compare(a,b){if(a.status!=='scenario'||b.status!=='scenario')return {comparable:false,reason:'Complete both source budgets before comparing.'};if(JSON.stringify(a.periods.map(p=>p.period))!==JSON.stringify(b.periods.map(p=>p.period)))return {comparable:false,reason:'Period labels differ. Align both sources to the same entered periods before comparing.'};return {comparable:true,marginDifference:b.margin-a.margin,changesBudgetFit:(a.margin>=0)!==(b.margin>=0),reason:'Comparison concerns the entered water budget only; access, quality, treatment and transport feasibility remain unverified.'};}
return {balance,compare};});
